const initialState = {
	// Define your initial state here
};

const mainReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default mainReducer;
